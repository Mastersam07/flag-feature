name: flag_feature
description: App wide feature flag manager utilizing the power of Firebase Remote Config.
version: 2.1.0
homepage: https://github.com/Mastersam07/flag-feature

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  firebase_remote_config: ^4.3.15
  flutter:
    sdk: flutter
  

dev_dependencies:
  flutter_lints: ^3.0.1
  flutter_test:
    sdk: flutter
  mockito: ^5.2.0
